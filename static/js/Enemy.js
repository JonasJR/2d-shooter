class Enemy {
  constructor(id, x, y, radius, angle) {
    this.id = id;
    this.x = x;
    this.y = y;
    this.radius = radius;
    this.angle = angle;
    this.pipeLength = PIPE_LENGTH;
    this.pipeWidth = PIPE_WIDTH;
  }
}
